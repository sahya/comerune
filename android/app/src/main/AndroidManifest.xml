<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="comerune"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links: receive the OIDC authorization callback after upstream redirect.
                 The host is build-time injected via the bffHost manifest placeholder
                 (sourced from android/oauth_bff.env — see oauth_bff.env.example).
                 The same host must publish assetlinks.json under
                 /.well-known/assetlinks.json so Android can auto-verify and intercept the
                 callback URL into this app instead of the browser. autoVerify="true"
                 triggers Google's offline verification on app install. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="${bffHost}"
                      android:pathPrefix="/auth/callback"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Foreground service for keeping the comment WebSocket connection alive
             while the app is backgrounded. dataSync matches actual usage; mediaPlayback
             would risk Android 14+ revocation since no media is continuously played.

             stopWithTask=true: when the user swipes the app away from the recents
             list, Android stops this service automatically so the foreground
             service notification disappears with the task. The Dart-level
             ForegroundTaskOptions.stopWithTask is intentionally NOT used because
             it triggers TrackVisibilityUtils — which stops the service on every
             activity pause (e.g. pressing home), breaking legitimate background
             streaming. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="true"
            android:exported="false" />
        <!-- Notification icon resource reference for the foreground service notification.
             Provides a complete notification on all Android versions and stabilizes
             foreground service startup. -->
        <meta-data
            android:name="com.example.comerune.service.NOTIFICATION_ICON"
            android:resource="@mipmap/ic_launcher" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
